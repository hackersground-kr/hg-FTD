<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error <%= errorCode %></title>
    <style>
        body {
        user-select: none;
        }
    </style>
</head>
<body>
    <%- include('header.ejs') %>
    <div style="margin-left: 30px;">
        <% if (errorCode === 404) { %>
            <h1>404: 페이지를 찾을 수 없습니다.</h1>
        <% } else if (errorCode === 500) { %>
            <h1>500: 서버 내부 오류입니다.</h1>
        <% } else { %>
            <h1><%= errorCode %>: 예기치 못한 오류가 발생했습니다.</h1>
        <% } %>
    </div>
</body>
</html>
